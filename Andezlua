-- [[ ANDEZ VIP ⚡ V7.9 FINAL HYBRID - PERCENTAGE EDITION ]]
-- Gabungan Animasi Persentase + Fitur Lengkap (Fly, SASA, Hack)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")
local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera

-- [[ 1. FUNGSI UTAMA ]]
local function MakeNeon(obj)
    task.spawn(function()
        local h = 0
        while obj and obj.Parent do
            h = h + 0.01; if h >= 1 then h = 0 end
            if obj:IsA("TextLabel") or obj:IsA("TextButton") then
                obj.TextColor3 = Color3.fromHSV(h, 1, 1)
            end
            task.wait(0.05)
        end
    end)
end

local function TotalKill()
    _G.nc = false; _G.SpecLoop = false
    if _G.NoclipConn then _G.NoclipConn:Disconnect() _G.NoclipConn = nil end
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid") then
        Camera.CameraSubject = LocalPlayer.Character.Humanoid
    end
    for _, ui in pairs(game.CoreGui:GetChildren()) do if ui.Name:find("AndezVip") then ui:Destroy() end end
end

-- [[ 2. SETUP SCREEN GUI ]]
local ScreenGui = Instance.new("ScreenGui", game.CoreGui)
ScreenGui.Name = "AndezVip_Hybrid"; ScreenGui.ResetOnSpawn = false

local LogoBtn = Instance.new("TextButton", ScreenGui)
LogoBtn.Size = UDim2.new(0, 45, 0, 45); LogoBtn.Position = UDim2.new(0.05, 0, 0.45, 0)
LogoBtn.BackgroundColor3 = Color3.fromRGB(20, 0, 0); LogoBtn.Text = "A"; LogoBtn.Font = "GothamBold"; LogoBtn.TextSize = 25
LogoBtn.Visible = false; LogoBtn.Active = true; LogoBtn.Draggable = true; Instance.new("UICorner", LogoBtn)
MakeNeon(LogoBtn)

local MainFrame = Instance.new("Frame", ScreenGui)
MainFrame.Size = UDim2.new(0, 190, 0, 320); MainFrame.Position = UDim2.new(0.7, 0, 0.25, 0)
MainFrame.BackgroundColor3 = Color3.fromRGB(5, 5, 5); MainFrame.Visible = false; MainFrame.Active = true; MainFrame.Draggable = true; Instance.new("UICorner", MainFrame)

-- [[ 3. ANIMASI LOADING PERSENTASE (BARU) ]]
task.spawn(function()
    local LoadingFrame = Instance.new("Frame", ScreenGui)
    LoadingFrame.Size = UDim2.new(0, 420, 0, 200); LoadingFrame.Position = UDim2.new(0.5, -210, 0.5, -100)
    LoadingFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 10); LoadingFrame.ClipsDescendants = true
    Instance.new("UICorner", LoadingFrame).CornerRadius = UDim.new(0, 15)
    local Stroke = Instance.new("UIStroke", LoadingFrame); Stroke.Color = Color3.fromRGB(255, 0, 0); Stroke.Thickness = 2

    local LogoBox = Instance.new("Frame", LoadingFrame)
    LogoBox.Size = UDim2.new(0, 110, 1, -20); LogoBox.Position = UDim2.new(0, 10, 0, 10)
    LogoBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20); Instance.new("UICorner", LogoBox)

    local BigA = Instance.new("TextLabel", LogoBox)
    BigA.Size = UDim2.new(1, 0, 1, 0); BigA.BackgroundTransparency = 1; BigA.Text = "A"; BigA.TextColor3 = Color3.new(1, 1, 1); BigA.Font = "GothamBlack"; BigA.TextSize = 100
    local Grad = Instance.new("UIGradient", BigA)
    Grad.Color = ColorSequence.new({ColorSequenceKeypoint.new(0, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 0, 0)), ColorSequenceKeypoint.new(0.51, Color3.fromRGB(255, 255, 255)), ColorSequenceKeypoint.new(1, Color3.fromRGB(255, 255, 255))}); Grad.Rotation = 90

    local AccessTitle = Instance.new("TextLabel", LoadingFrame)
    AccessTitle.Size = UDim2.new(0.65, 0, 0, 40); AccessTitle.Position = UDim2.new(0.3, 0, 0.1, 0); AccessTitle.Text = "ANDEZ VIP ACCESS"; AccessTitle.TextColor3 = Color3.fromRGB(255, 0, 0); AccessTitle.Font = "GothamBlack"; AccessTitle.TextSize = 22; AccessTitle.BackgroundTransparency = 1; MakeNeon(AccessTitle)

    local StatusText = Instance.new("TextLabel", LoadingFrame)
    StatusText.Size = UDim2.new(0.65, 0, 0, 30); StatusText.Position = UDim2.new(0.3, 0, 0.4, 0); StatusText.Text = "INITIALIZING..."; StatusText.TextColor3 = Color3.new(1, 1, 1); StatusText.BackgroundTransparency = 1; StatusText.Font = "GothamBlack"; StatusText.TextSize = 16

    local PercentText = Instance.new("TextLabel", LoadingFrame)
    PercentText.Size = UDim2.new(0.65, 0, 0, 30); PercentText.Position = UDim2.new(0.3, 0, 0.55, 0); PercentText.Text = "0%"; PercentText.TextColor3 = Color3.fromRGB(255, 0, 0); PercentText.BackgroundTransparency = 1; PercentText.Font = "GothamBlack"; PercentText.TextSize = 24

    local BarBg = Instance.new("Frame", LoadingFrame)
    BarBg.Size = UDim2.new(0.6, 0, 0, 8); BarBg.Position = UDim2.new(0.32, 0, 0.8, 0); BarBg.BackgroundColor3 = Color3.fromRGB(30, 30, 30); Instance.new("UICorner", BarBg)
    local BarFill = Instance.new("Frame", BarBg)
    BarFill.Size = UDim2.new(0, 0, 1, 0); BarFill.BackgroundColor3 = Color3.fromRGB(255, 0, 0); Instance.new("UICorner", BarFill)

    local duration = 4
    local tw = TweenService:Create(BarFill, TweenInfo.new(duration, Enum.EasingStyle.Linear), {Size = UDim2.new(1, 0, 1, 0)})
    tw:Play()

    task.spawn(function()
        local startTick = tick()
        while tick() - startTick < duration do
            local progress = math.floor(((tick() - startTick) / duration) * 100)
            PercentText.Text = progress .. "%"
            if progress < 30 then StatusText.Text = "SCANNING DATA..."
            elseif progress < 70 then StatusText.Text = "BYPASSING SECURITY..."
            else StatusText.Text = "SUCCESS!" end
            task.wait()
        end
        PercentText.Text = "100%"; StatusText.Text = "READY TO USE!"; StatusText.TextColor3 = Color3.fromRGB(0, 255, 0)
    end)

    tw.Completed:Connect(function()
        task.wait(0.5); LoadingFrame:Destroy(); LogoBtn.Visible = true
    end)
end)

-- [[ 4. NAVIGASI & TITLE BAR ]]
local TitleBar = Instance.new("Frame", MainFrame)
TitleBar.Size = UDim2.new(1, 0, 0, 35); TitleBar.BackgroundColor3 = Color3.fromRGB(120, 0, 0); Instance.new("UICorner", TitleBar)
local TitleText = Instance.new("TextLabel", TitleBar)
TitleText.Text = "ANDEZ VIP"
TitleText.Size = UDim2.new(0.6, 0, 0.7, 0) 
TitleText.Position = UDim2.new(0.05, 0, 0.15, 0) 
TitleText.Font = Enum.Font.GothamBlack
TitleText.TextScaled = true -- Teks otomatis besar sesuai kotak tanpa terpotong
TitleText.BackgroundTransparency = 1
TitleText.TextColor3 = Color3.new(1,1,1)
TitleText.TextXAlignment = Enum.TextXAlignment.Left 
local MainClose = Instance.new("TextButton", TitleBar); MainClose.Text = "X"; MainClose.Size = UDim2.new(0, 25, 0, 25); MainClose.Position = UDim2.new(1, -30, 0, 5); MainClose.BackgroundColor3 = Color3.fromRGB(200, 0, 0); MainClose.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", MainClose); MainClose.MouseButton1Click:Connect(TotalKill)
local MainMin = Instance.new("TextButton", TitleBar); MainMin.Text = "-"; MainMin.Size = UDim2.new(0, 25, 0, 25); MainMin.Position = UDim2.new(1, -60, 0, 5); MainMin.BackgroundColor3 = Color3.fromRGB(40, 40, 40); MainMin.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", MainMin)

MainMin.MouseButton1Click:Connect(function()
    local tw = TweenService:Create(MainFrame, TweenInfo.new(0.4, Enum.EasingStyle.Back, Enum.EasingDirection.In), {Size = UDim2.new(0,0,0,0)})
    tw:Play(); tw.Completed:Connect(function() MainFrame.Visible = false; LogoBtn.Visible = true end)
end)

LogoBtn.MouseButton1Click:Connect(function()
    MainFrame.Size = UDim2.new(0,0,0,0); MainFrame.Visible = true; LogoBtn.Visible = false
    TweenService:Create(MainFrame, TweenInfo.new(0.5, Enum.EasingStyle.Back), {Size = UDim2.new(0, 190, 0, 320)}):Play()
end)

local Scroll = Instance.new("ScrollingFrame", MainFrame); Scroll.Size = UDim2.new(1, -10, 1, -45); Scroll.Position = UDim2.new(0, 5, 0, 40); Scroll.BackgroundTransparency = 1; Scroll.ScrollBarThickness = 2; Instance.new("UIListLayout", Scroll).Padding = UDim.new(0, 6)

-- [[ 5. FITUR SASA TP & SPECTATOR ]]
local function CreateRestoreBtn(txt, pos, color)
    local b = Instance.new("TextButton", ScreenGui); b.Size = UDim2.new(0, 45, 0, 45); b.Position = pos; b.BackgroundColor3 = Color3.fromRGB(10,10,10); b.Text = txt; b.TextColor3 = color; b.Font = "GothamBold"; b.TextSize = 10; b.Visible = false; b.Draggable = true; Instance.new("UICorner", b); return b
end
local SasaTPRestore = CreateRestoreBtn("SASA TP", UDim2.new(0.92, 0, 0.4, 0), Color3.fromRGB(0, 255, 150))
local SasaSpecRestore = CreateRestoreBtn("SASA 👀", UDim2.new(0.92, 0, 0.55, 0), Color3.fromRGB(255, 105, 180))

local function CreateSasaPanel(name, color, restore)
    local f = Instance.new("Frame", ScreenGui); f.Size = UDim2.new(0, 195, 0, 260); f.Position = UDim2.new(0.4, 0, 0.3, 0); f.BackgroundColor3 = Color3.fromRGB(10, 10, 10); f.Visible = false; f.Active = true; f.Draggable = true; Instance.new("UICorner", f)
    local h = Instance.new("Frame", f); h.Size = UDim2.new(1, 0, 0, 35); h.BackgroundColor3 = Color3.fromRGB(20, 20, 20); Instance.new("UICorner", h)
    local t = Instance.new("TextLabel", h); t.Text = name; t.TextColor3 = color; t.Size = UDim2.new(0.6, 0, 1, 0); t.Position = UDim2.new(0.05, 0, 0, 0); t.BackgroundTransparency = 1; t.Font = "GothamBold"; t.TextSize = 11; t.TextXAlignment = "Left"
    local c = Instance.new("TextButton", h); c.Text = "X"; c.Size = UDim2.new(0, 22, 0, 22); c.Position = UDim2.new(0.85, 0, 0.15, 0); c.BackgroundColor3 = Color3.fromRGB(150, 0, 0); c.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", c); c.MouseButton1Click:Connect(function() f.Visible = false; restore.Visible = false end)
    local m = Instance.new("TextButton", h); m.Text = "-"; m.Size = UDim2.new(0, 22, 0, 22); m.Position = UDim2.new(0.7, 0, 0.15, 0); m.BackgroundColor3 = Color3.fromRGB(50, 50, 50); m.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", m); m.MouseButton1Click:Connect(function() f.Visible = false; restore.Visible = true end)
    restore.MouseButton1Click:Connect(function() f.Visible = true; restore.Visible = false end)
    local s = Instance.new("ScrollingFrame", f); s.BackgroundTransparency = 1; s.ScrollBarThickness = 2; Instance.new("UIListLayout", s).Padding = UDim.new(0, 5); s.Position = UDim2.new(0.05, 0, 0.18, 0); s.Size = UDim2.new(0.9, 0, 0.77, 0)
    return f, s
end
local SasaTPFrame, SasaTPScroll = CreateSasaPanel("SASA TP VIP", Color3.fromRGB(0, 255, 150), SasaTPRestore)
local SasaSpectFrame, SasaSpectScroll = CreateSasaPanel("SASA SPECTATOR", Color3.fromRGB(255, 105, 180), SasaSpecRestore)

local function updateLists()
    for _, child in pairs(SasaTPScroll:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end
    for _, child in pairs(SasaSpectScroll:GetChildren()) do if child:IsA("Frame") then child:Destroy() end end
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            local fTP = Instance.new("Frame", SasaTPScroll); fTP.Size = UDim2.new(1, 0, 0, 35); fTP.BackgroundColor3 = Color3.fromRGB(25, 25, 25); Instance.new("UICorner", fTP)
            local bTP = Instance.new("TextButton", fTP); bTP.Size = UDim2.new(0, 40, 0.7, 0); bTP.Position = UDim2.new(1, -45, 0.15, 0); bTP.Text = "TP"; bTP.BackgroundColor3 = Color3.fromRGB(0, 200, 100); Instance.new("UICorner", bTP); bTP.MouseButton1Click:Connect(function() if p.Character then LocalPlayer.Character.HumanoidRootPart.CFrame = p.Character.HumanoidRootPart.CFrame end end)
            local nTP = Instance.new("TextLabel", fTP); nTP.Text = p.Name; nTP.Size = UDim2.new(1, -55, 1, 0); nTP.TextColor3 = Color3.new(1,1,1); nTP.BackgroundTransparency = 1; nTP.TextXAlignment = "Left"

            local fSP = Instance.new("Frame", SasaSpectScroll); fSP.Size = UDim2.new(1, 0, 0, 45); fSP.BackgroundColor3 = Color3.fromRGB(45, 25, 35); Instance.new("UICorner", fSP)
            local bSP_EYE = Instance.new("TextButton", fSP); bSP_EYE.Size = UDim2.new(0, 45, 0.7, 0); bSP_EYE.Position = UDim2.new(0.7, 0, 0.15, 0); bSP_EYE.Text = "EYE"; bSP_EYE.BackgroundColor3 = Color3.fromRGB(255, 182, 193); Instance.new("UICorner", bSP_EYE)
            bSP_EYE.MouseButton1Click:Connect(function() if _G.SpecLoop then _G.SpecLoop = false; Camera.CameraSubject = LocalPlayer.Character.Humanoid else _G.SpecLoop = true; task.spawn(function() while _G.SpecLoop do if p.Character then Camera.CameraSubject = p.Character.Humanoid end task.wait(0.1) end end) end end)
        end
    end
end
Players.PlayerAdded:Connect(updateLists); Players.PlayerRemoving:Connect(updateLists); updateLists()

-- [[ 6. FLY CONTROL ]]
local FlyUI = Instance.new("Frame", ScreenGui); FlyUI.Size = UDim2.new(1, 0, 1, 0); FlyUI.BackgroundTransparency = 1; FlyUI.Visible = false 
local FlyPanel = Instance.new("Frame", FlyUI); FlyPanel.Size = UDim2.new(0, 160, 0, 115); FlyPanel.Position = UDim2.new(0.4, 0, 0.2, 0); FlyPanel.BackgroundColor3 = Color3.fromRGB(15, 15, 15); FlyPanel.Active = true; FlyPanel.Draggable = true; Instance.new("UICorner", FlyPanel)
local FlyClose = Instance.new("TextButton", FlyPanel); FlyClose.Text = "X"; FlyClose.Size = UDim2.new(0, 22, 0, 22); FlyClose.Position = UDim2.new(1, -25, 0, 2); FlyClose.BackgroundColor3 = Color3.fromRGB(180, 0, 0); FlyClose.MouseButton1Click:Connect(function() FlyUI.Visible = false end)
local FlyToggle = Instance.new("TextButton", FlyPanel); FlyToggle.Size = UDim2.new(0.9, 0, 0, 30); FlyToggle.Position = UDim2.new(0.05, 0, 0.3, 0); FlyToggle.Text = "FLY: OFF"; FlyToggle.BackgroundColor3 = Color3.fromRGB(130,0,0); FlyToggle.TextColor3 = Color3.new(1,1,1); Instance.new("UICorner", FlyToggle)

local ArrowFrame = Instance.new("Frame", FlyUI); ArrowFrame.Size = UDim2.new(0, 180, 0, 180); ArrowFrame.Position = UDim2.new(0.08, 0, 0.4, 0); ArrowFrame.BackgroundTransparency = 1; ArrowFrame.Visible = false 
local function MakeArrow(txt, pos)
    local b = Instance.new("TextButton", ArrowFrame); b.Text = txt; b.Size = UDim2.new(0, 50, 0, 50); b.Position = pos; b.BackgroundColor3 = Color3.fromRGB(0,0,0); b.BackgroundTransparency = 0.4; b.TextColor3 = Color3.new(1,1,1); b.Font = "GothamBold"; b.TextSize = 25; Instance.new("UICorner", b); return b
end
local Up = MakeArrow("▲", UDim2.new(0.35, 0, 0, 0)); local Down = MakeArrow("▼", UDim2.new(0.35, 0, 0.7, 0)); local Left = MakeArrow("◀", UDim2.new(0, 0, 0.35, 0)); local Right = MakeArrow("▶", UDim2.new(0.7, 0, 0.35, 0))

local flying, moveDir, bv, bg = false, Vector3.new(0,0,0), nil, nil
FlyToggle.MouseButton1Click:Connect(function()
    flying = not flying; FlyToggle.Text = flying and "FLY: ON" or "FLY: OFF"; FlyToggle.BackgroundColor3 = flying and Color3.fromRGB(0, 130, 0) or Color3.fromRGB(130, 0, 0); ArrowFrame.Visible = flying 
    local hrp = LocalPlayer.Character:WaitForChild("HumanoidRootPart")
    if flying then 
        LocalPlayer.Character.Humanoid.PlatformStand = true; bv = Instance.new("BodyVelocity", hrp); bv.MaxForce = Vector3.new(9e9,9e9,9e9); bg = Instance.new("BodyGyro", hrp); bg.MaxTorque = Vector3.new(9e9,9e9,9e9)
        task.spawn(function() while flying do bv.Velocity = (moveDir.Magnitude > 0) and (Camera.CFrame:VectorToWorldSpace(moveDir) * 150) or Vector3.new(0,0.1,0); bg.CFrame = Camera.CFrame; task.wait() end end)
    else if bv then bv:Destroy() end; if bg then bg:Destroy() end; LocalPlayer.Character.Humanoid.PlatformStand = false end
end)
Up.MouseButton1Down:Connect(function() moveDir = Vector3.new(0, 0, -1) end); Up.MouseButton1Up:Connect(function() moveDir = Vector3.new(0, 0, 0) end)
Down.MouseButton1Down:Connect(function() moveDir = Vector3.new(0, 0, 1) end); Down.MouseButton1Up:Connect(function() moveDir = Vector3.new(0, 0, 0) end)
Left.MouseButton1Down:Connect(function() moveDir = Vector3.new(-1, 0, 0) end); Left.MouseButton1Up:Connect(function() moveDir = Vector3.new(0, 0, 0) end)
Right.MouseButton1Down:Connect(function() moveDir = Vector3.new(1, 0, 0) end); Right.MouseButton1Up:Connect(function() moveDir = Vector3.new(0, 0, 0) end)

-- [[ 7. LIST FITUR ]]
local function AddButton(name, callback)
    local b = Instance.new("TextButton", Scroll); b.Size = UDim2.new(0.95, 0, 0, 32); b.BackgroundColor3 = Color3.fromRGB(40,40,40); b.Text = name; b.TextColor3 = Color3.new(1,1,1); b.Font = "GothamBold"; b.TextSize = 10; Instance.new("UICorner", b); b.MouseButton1Click:Connect(callback)
end
local function AddToggle(name, callback)
    local active = false; local b = Instance.new("TextButton", Scroll); b.Size = UDim2.new(0.95, 0, 0, 32); b.BackgroundColor3 = Color3.fromRGB(40,40,40); b.Text = "[ OFF ] " .. name; b.TextColor3 = Color3.new(1,1,1); b.Font = "GothamBold"; b.TextSize = 10; Instance.new("UICorner", b)
    b.MouseButton1Click:Connect(function() active = not active; b.Text = (active and "[ ON ] " or "[ OFF ] ") .. name; b.BackgroundColor3 = active and Color3.fromRGB(0, 120, 0) or Color3.fromRGB(40,40,40); callback(active) end)
end

AddButton("FLY CONTROL", function() FlyUI.Visible = true end)
AddButton("SASA TP", function() SasaTPFrame.Visible = true end)
AddButton("SASA SPECT", function() SasaSpectFrame.Visible = true end)
AddToggle("Speed Hack", function(v) LocalPlayer.Character.Humanoid.WalkSpeed = v and 100 or 16 end)
AddToggle("Jump Hack", function(v) LocalPlayer.Character.Humanoid.JumpPower = v and 150 or 50 end)
AddToggle("ESP Wallhack", function(v) if v then for _,p in pairs(Players:GetPlayers()) do if p ~= LocalPlayer and p.Character then local hl = Instance.new("Highlight", p.Character); hl.FillColor = Color3.new(1,0,0) end end end end)
AddToggle("Noclip (Tembus)", function(v) _G.nc = v; if v then _G.NoclipConn = RunService.Stepped:Connect(function() if _G.nc and LocalPlayer.Character then for _, p in pairs(LocalPlayer.Character:GetDescendants()) do if p:IsA("BasePart") then p.CanCollide = false end end end end) end end)

print("ANDEZ VIP V7.9 PERCENTAGE HYBRID LOADED!")
